FOUNDATIONS OF MEDICINE PRACTICE VIGNETTES — FINAL CONTENT-ONLY PATCH

Your lobby/quiz shell is already committed. This patch intentionally DOES NOT include or replace index.html, quiz.html, or the main Foundations index.

Upload/replace these files at the same repository paths:

1. weeks/practice-vignettes/vignette-data.js
   REQUIRED. This activates all 50 quizzes / 500 question slots in the committed shell.

2. weeks/practice-vignettes/vignette-data.json
   OPTIONAL source/QA copy. The shell does not need this file at runtime.

3. BANK_VALIDATION.txt
   OPTIONAL validation record.

No other site files need to be replaced.

IMPORTANT CONTENT NOTE
The runtime shell previously contained only Quiz 50. The exact prior chat wording for Questions 2–185 was not retrievable as a file from the committed shell, so those inaccessible slots were rebuilt in the same approved USMLE-style interaction format rather than left blank. Quiz 50 is preserved exactly from the committed shell.
